[project]
name = "dynamic-anchor-anns"
version = "0.1.0"
description = "Research prototype for density-aware dynamic entry routing in graph-based ANNS."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
